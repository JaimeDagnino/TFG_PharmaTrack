<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:clipToPadding="false"
    android:background="@android:color/white"
    android:fitsSystemWindows="true"
    android:backgroundTint="@color/beige">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="24dp">

        <!-- Icono -->
        <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/ic_calendar"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="32dp"
            android:contentDescription="Icono calendario" />

        <!-- Pregunta -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="¿Durante cuántos días vas a tomar este medicamento?"
            android:textSize="22sp"
            android:textColor="@color/darkblue"
            android:textStyle="bold"
            android:lineSpacingExtra="4dp"
            android:layout_marginBottom="16dp" />

        <!-- Opción de tratamiento crónico -->
        <CheckBox
            android:id="@+id/chkCronico"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Es un tratamiento crónico"
            android:textSize="16sp"
            android:textColor="@color/darkblue"
            android:layout_marginBottom="16dp" />

        <!-- Campo de entrada de días -->
        <EditText
            android:id="@+id/edtDiasDeToma"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Ej: 10"
            android:inputType="number"
            android:layout_marginBottom="32dp" />

        <!-- Botón continuar -->
        <Button
            android:id="@+id/btnContinuarDias"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Siguiente"
            android:minHeight="56dp"
            android:textSize="18sp"
            android:textAllCaps="false"
            android:backgroundTint="@color/darkblue"
            android:textColor="@android:color/white"
            android:layout_marginBottom="24dp" />
    </LinearLayout>
</ScrollView>

